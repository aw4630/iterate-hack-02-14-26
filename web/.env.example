# Copy this file to .env and fill in your values.

# For detection (component boxing): use either a Google Gemini key or a Dedalus key (dsk-...).
# Dedalus: https://dedaluslabs.ai – use your credit for detection.
VITE_GEMINI_API_KEY=dsk-test-0c04ac110a33-3b890e7f45981c225d18f98419302d01

# Optional: Dedalus key for voice "Ask about this component" only. If set, voice Q&A uses Dedalus (not Gemini).
# If unset, voice uses VITE_GEMINI_API_KEY (Dedalus when key is dsk-..., else Gemini).
VITE_DEDALUS_VOICE_API_KEY=dsk-test-0c04ac110a33-3b890e7f45981c225d18f98419302d01

# Dedalus model for voice Q&A only (other features use Gemini 2.0 Flash). Default: mistralai/mistral-small.
# VITE_DEDALUS_VOICE_MODEL=mistralai/mistral-small

# For Gemini Live (voice + vision): must be a Google API key. Only needed if you use "Start Gemini Live".
# If you use Dedalus for detection, add a Google key here for Live: https://aistudio.google.com/apikey
# VITE_GEMINI_LIVE_API_KEY=YOUR_GOOGLE_API_KEY

# Optional: AeroDetect YOLO server for aircraft component detection
# Run aero-detect-server and set this to use YOLO detection instead of Gemini for boxes.
# VITE_AERODETECT_API_URL=http://localhost:5000

# Optional: OpenClaw for "execute" actions (search manuals, file reports, etc.)
# See README for OpenClaw setup. Use your Mac's Bonjour hostname.
VITE_OPENCLAW_HOST=http://YOUR_MAC_HOSTNAME.local
VITE_OPENCLAW_PORT=18789
VITE_OPENCLAW_GATEWAY_TOKEN=YOUR_OPENCLAW_GATEWAY_TOKEN
